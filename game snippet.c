void processFlag(tFlag *pFlag, tFlag *pEnemyFlag) {
	
	if(grabbed) {
		if(/* pFlag position near pEnemyFlag position*/) {
			// TODO: score point
		}
		
		// TODO: Draw flag on jeep
	}
	else {
		// TODO: Draw flag stuck to the ground
	}
}